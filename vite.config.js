import { defineConfig } from 'vite'

// Adjust the base path so the app works when served from GitHub Pages
// Replace 'class-finder' if your repository name is different.
export default defineConfig({
  base: '/class-finder/',
})
